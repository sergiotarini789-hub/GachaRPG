# Queue settings

# Maximum number of tasks one autonomous session may process before it
# stops safely and reports (completed / failed / blocked / remaining).
# Remove the line to fall back to the built-in default of 5.
MAX_AUTONOMOUS_TASKS=5
